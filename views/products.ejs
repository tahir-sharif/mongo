<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <% if(product.length ){ %>
        <ul>
            <% product.forEach((prdctObj)=> { %>
                <li>
                    <h3>
                        <%= prdctObj.title %>
                            <%= prdctObj.price %>
                    </h3>
                </li>
                <% }) %>
        </ul>
        <%}else{ %>
            <h3>No Product</h3>
            <%} %>
</body>

</html>